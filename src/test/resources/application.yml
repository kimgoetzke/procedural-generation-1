spring:
  profiles:
    active: cli-dev
    include: worldtest
logging:
  level:
    root: INFO
settings:
  environment: CLI
  general-properties:
    auto-unload: true
    clear-console: false
    use-console-ui: false
